import styles from '../styles/Navbar.module.scss';

const Navbar = () => {
    return (
        <nav className={styles.container}>
            <ul>
                <li>1</li>
            </ul>
        </nav>
    );
};

export default Navbar;